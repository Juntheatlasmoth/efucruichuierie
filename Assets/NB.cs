using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NB : MonoBehaviour
{
    int j = 10;
    int a;
                    float fractional = 3.6f;
         double number = 3.7;
    string text = "Hello";
    bool isRunning = true;



    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        print("ERROR: Keyboard not detected. Forward by pressing any key. Search diagnostics with F5.");
        print(j);
        print("print(print");
    }
}
